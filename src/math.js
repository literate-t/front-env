// // var math = math || {};
// const math = {};

// (() => {
//   const sum = (a, b) => {
//     return a + b;
//   };

//   math.sum = sum;
// })();

export const sum = (a, b) => a + b;
